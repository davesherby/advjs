pipeline {
    agent any
      tools {nodejs "node9"}
      stages {
          stage("foo") {
              steps {
                  echo "coucou"
    		  echo "npm -v"
		  echo "au revoir"
              }
          }
      }
}
